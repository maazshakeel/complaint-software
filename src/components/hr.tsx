import React, { FC } from 'react'
import { View } from 'react-native'

const HL: FC = () => {
  return (
    // @ts-ignore
    <View
      style={{
        borderBottomColor: 'B2ACAC',
        borderBottomWidth: 1
      }}
    />
  )
}

export default HL
