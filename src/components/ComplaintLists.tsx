import React, { FC } from 'react'
import { ScrollView } from 'react-native'

const ComplaintList: FC = () => {
  return (
    <ScrollView>
      {/* Row  */}
      <Text></Text>
    </ScrollView>
  )
}
